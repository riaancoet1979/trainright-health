node_modules/
dist/
npm-install.log
.garmin_tokens/
.vite-temp/
__pycache__/

# Personal health data exported by garmin_sync.py — never commit or deploy.
# GitHub workflows validate that generated payloads are absent from Pages artifacts.
# See audit/SECURITY_PRIVACY.md.
public/gh-sync.json
/gh-sync.json
# Legacy filename (pre-Phase-1 rename) — kept so old workflows can't leak it.
public/garmin_health.json
/garmin_health.json

# User-local helper scripts (push_*.bat, fix_*.bat) — preserved but not tracked
/*.local.bat
